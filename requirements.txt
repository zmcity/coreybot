# Runtime dependencies for coreybot (pinned to the versions this project is tested against).
# The framework core (core/, llm/, runtime/) is pure standard library; the
# frontends (TUI + CLI rendering) need textual/rich, and the ``calc`` builtin
# uses SymPy for its algebra/scientific engine.
textual==8.2.8
rich==15.0.0
sympy==1.14.0
mpmath==1.3.0
